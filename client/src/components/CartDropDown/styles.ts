import styled from 'styled-components'

import { Wrapper as CartWrapperStyled } from 'components/CartList/styles'

export const Wrapper = styled.main`
  ${CartWrapperStyled} {
    width: 56rem;
  }
`
